#include "common.h"
#include <QByteArray>
#include <QDebug>
#include <QtEndian>

void hexStringTest()
{
    QByteArray ba;
    //ba.append(255);

    //if ((quint8)ba.at(0) == 0xFF)
    //    qDebug() << "0xFF";
    //else
    //    qDebug() << ba.at(0);

    //qDebug() << ba.toHex();
    //qDebug() << ba.size();
    //qDebug() << ba.toHex().size();

    //ba = { (char)0xff, (char)0xff };
    //ba.append(255).append(255);
    ////ba.append("ffff");
    ////qDebug() << ba;
    ////qDebug() << ba.toHex();
    ////bool ok = 1;
    ////qDebug() << ba.toShort(&ok, 16);
    ////qDebug() << ba.toShort(&ok);

    //
    ////QByteArray ba2 = qFromBigEndian(ba);
    //short i = 0x4477;
    //short i2 = qFromLittleEndian(i);
    //qDebug() << hex << i2;

    while (-1)
    {
        qDebug() << "-1";
    }
}
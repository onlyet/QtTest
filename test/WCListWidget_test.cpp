#include "common.h"
#include "WCListWidget.h"
#include <qvariant.h>
#include <qlistwidget.h>

void WCListWidget_test()
{
    //int w = 1400;

    //int columnCount = (w - 465) / 317;
    //int itemWidth = (w - 465) / columnCount;
    ////QVariantList studentList = CONFIG.studentList();

    //WCListWidget *m_padList;

    //for (int i = 0; i < 8; i++)
    //{
    //    QListWidgetItem *item = new QListWidgetItem(m_padList);
    //    item->setSizeHint(QSize(itemWidth - 8, 500));
    //    StudentNoteNamesItem *penItem = new StudentNoteNamesItem(m_padList, m_allList[i], QStringLiteral("¹²1Ò³"));
    //    CanvasWidget *padWidget = new CanvasWidget();
    //    penItem->addPenWidget(padWidget);
    //    padWidget->setFixedSize(QSize(313, 444));
    //    padWidget->setProperty("studentName", m_allList[i]);
    //    padWidget->setDrawEnabled(false);
    //    connect(padWidget, SIGNAL(doubleClicked()), this, SLOT(padWidgetDoubleClicked()));
    //    m_penWidgets.insert(studentList[i].toMap()["dxid"].toString(), padWidget);
    //    m_nameItems.insert(studentList[i].toMap()["dxid"].toString(), penItem);
    //    m_padList->setItemWidget(item, penItem);
    //}
}